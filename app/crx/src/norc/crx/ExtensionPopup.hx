package norc.crx;

import jabber.JID;

class ExtensionPopup {

	static var ext : IExtension;

	static function main() {
		trace( 'popadipop' );

		//ext = chrome.Extension.getBackgroundPage();
		//trace(ext);
	}

}
